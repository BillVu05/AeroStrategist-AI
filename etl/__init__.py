"""
Data pipeline: fetch real reference data, derive the observations the
market model trains on.

These are scripts, not library code - run them with -m from the repo root:

    python -m etl.fetch_airports
    python -m etl.generate_synthetic_demand
"""
