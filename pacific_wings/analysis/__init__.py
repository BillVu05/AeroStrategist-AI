"""Screening for destinations Pacific Wings does not fly yet.

`open_route` estimates a market for an unobserved route and then hands
off to `simulation.engine` for the allocation and the financials, so a
screening run and a scenario run cannot disagree.
"""
